[pytest]
testpaths = tests
markers =
    slow: exhaustive sweeps that take minutes; deselected by default, run with -m slow
# The exhaustive port-pair matrix is 600 route solves at 25 ports. Keep it out
# of the default run so the suite stays usable, and opt in with `-m slow`.
addopts = -m "not slow"
